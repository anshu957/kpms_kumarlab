# Example Data

Small sample dataset for testing and demonstrations:
- 2 pose estimation H5 files
- 2 corresponding MP4 video files

For actual experiments, organize data externally and use symlinks in `data/`.
